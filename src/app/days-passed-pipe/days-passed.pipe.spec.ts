import { DaysPassedPipe } from './DaysPassedPipe';

describe('DaysPassedPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysPassedPipe();
    expect(pipe).toBeTruthy();
  });
});
